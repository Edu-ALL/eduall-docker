# EDUALL START   
    server {
        listen 80;
        server_name edu-all.com;

        root /eduall/public;
        index index.php index.html;

        server_tokens off;

        # Security Headers
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
        add_header Content-Security-Policy "default-src 'self' https: data: blob: 'unsafe-inline' 'unsafe-eval';" always;
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;
        add_header Content-Security-Policy "
            default-src 'self' https:;
            script-src 'self' 'unsafe-inline' https:;
            style-src 'self' 'unsafe-inline' https:;
            img-src 'self' https: data:;
            font-src 'self' https:;
            connect-src 'self' https:;
            object-src 'none';
        " always;
        fastcgi_hide_header X-Powered-By;

        # Serve static files directly
        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        # Laravel health check
        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }

        # PHP-FPM handling
        location ~ \.php$ {
            try_files $uri =404;
            fastcgi_split_path_info ^(.+\.php)(/.+)$;

            fastcgi_pass php_eduall:9000;
            fastcgi_index index.php;
            include fastcgi_params;

            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            fastcgi_param PATH_INFO $fastcgi_path_info;

            # Tambahkan baris ini supaya Laravel tahu request asli HTTPS
            fastcgi_param HTTPS $http_x_forwarded_proto;
            fastcgi_param HTTP_X_FORWARDED_PROTO $http_x_forwarded_proto;

            fastcgi_connect_timeout 14400;
            fastcgi_send_timeout 14400;
            fastcgi_read_timeout 14400;
        }

        # Deny access to hidden files
        location ~ /\.(?!well-known).* {
            deny all;
        }
    }
# EDUALL END

# REGISTRATION START
    server {
        listen 80;
        server_name registration.edu-all.com;
        index index.php index.html;
        root /registration/dist;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }
    }
# REGISTRATION END

# TAKE DOWN 
# JUNIOR START
    # server {
    #     listen 80;
    #     server_name junior.edu-all.com;
    #     return 301 https://$server_name$request_uri;
    # }

    # server {
    #     listen 443 ssl;

    #     server_name junior.edu-all.com;
    #     index index.php index.html;
    #     root /junior/public;

    #     ssl_certificate /etc/letsencrypt/live/edu-all.com-0002/fullchain1.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/edu-all.com-0002/privkey1.pem;

    #     # Load Security Config
    #     include /etc/nginx/security.conf;

    #     location / {
    #         try_files $uri $uri/ /index.php?$query_string;
    #     }

    #     location ~ \.php$ {
    #         try_files $uri = 404;
    #         fastcgi_split_path_info ^(.+\.php)(/.+)$;
    #         fastcgi_pass php_junior:9000;
    #         fastcgi_index index.php;
    #         include fastcgi_params;
    #         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    #         fastcgi_param PATH_INFO $fastcgi_path_info;
    #     }
    # }
# JUNIOR END

# TIMESHEET START
    server {
        listen 80;
        server_name timesheet.edu-all.com;
        root /timesheet/public;
        index index.php index.html;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }

        location ~ \.php$ {
            try_files $uri = 404;
            fastcgi_split_path_info ^(.+\.php)(/.+)$;
            fastcgi_pass php_timesheet:9000;
            fastcgi_index index.php;
            include fastcgi_params;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            fastcgi_param PATH_INFO $fastcgi_path_info;

            # Tambahkan baris ini supaya Laravel tahu request asli HTTPS
            fastcgi_param HTTPS $http_x_forwarded_proto;
            fastcgi_param HTTP_X_FORWARDED_PROTO $http_x_forwarded_proto;

            # --- FastCGI Timeout (10 minutes = 600 seconds) ---
            fastcgi_connect_timeout 600s;
            fastcgi_send_timeout    600s;
            fastcgi_read_timeout    600s;
        }
    }
# TIMESHEET END

# ASSESSMENT START
    server {
        listen 80;
        server_name assessment.edu-all.com;

        root /assessment/v2/public;
        index index.php index.html;

        # Serve static files directly
        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        # Laravel health check
        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }

        # PHP-FPM handling
        location ~ \.php$ {
            try_files $uri =404;
            fastcgi_split_path_info ^(.+\.php)(/.+)$;

            fastcgi_pass php_assessment:9000;
            fastcgi_index index.php;
            include fastcgi_params;

            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            fastcgi_param PATH_INFO $fastcgi_path_info;

            # Tambahkan baris ini supaya Laravel tahu request asli HTTPS
            fastcgi_param HTTPS $http_x_forwarded_proto;
            fastcgi_param HTTP_X_FORWARDED_PROTO $http_x_forwarded_proto;

            fastcgi_connect_timeout 14400;
            fastcgi_send_timeout 14400;
            fastcgi_read_timeout 14400;
        }

        # Deny access to hidden files
        location ~ /\.(?!well-known).* {
            deny all;
        }
    }
# ASSESSMENT END

# ASSESSMENT V1 START
    # server {
    #     listen 80;
    #     server_name assessment-v1.edu-all.com;
    #     return 301 https://$server_name$request_uri;
    # }

    # server {
    #     listen 443 ssl;

    #     server_name assessment-v1.edu-all.com;
    #     index index.php index.html;
    #     root /assessment/public;

    #     ssl_certificate /etc/letsencrypt/live/edu-all.com-0002/fullchain1.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/edu-all.com-0002/privkey1.pem;

    #     # Load Security Config
    #     include /etc/nginx/security.conf;

    #     location / {
    #         try_files $uri $uri/ /index.php?$query_string;
    #     }

    #     location ~ \.php$ {
    #         try_files $uri = 404;
    #         fastcgi_split_path_info ^(.+\.php)(/.+)$;
    #         fastcgi_pass php_assessment:9000;
    #         fastcgi_index index.php;
    #         include fastcgi_params;
    #         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    #         fastcgi_param PATH_INFO $fastcgi_path_info;
    #     }
    # }
# ASSESSMENT V1 END

# EDITING START
    # server {
    #     listen 80;
    #     server_name editing-v1.edu-all.com;
    #     return 301 https://$server_name$request_uri;
    # }

    # server {
    #     listen 443 ssl;

    #     server_name editing-v1.edu-all.com;
    #     index index.php index.html;
    #     root /editing/public;

    #     ssl_certificate /etc/letsencrypt/live/edu-all.com-0002/fullchain1.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/edu-all.com-0002/privkey1.pem;

    #     # Load Security Config
    #     include /etc/nginx/security.conf;

    #     location / {
    #         try_files $uri $uri/ /index.php?$query_string;
    #     }

    #     location ~ \.php$ {
    #         try_files $uri = 404;
    #         fastcgi_split_path_info ^(.+\.php)(/.+)$;
    #         fastcgi_pass php_editing:9000;
    #         fastcgi_index index.php;
    #         include fastcgi_params;
    #         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    #         fastcgi_param PATH_INFO $fastcgi_path_info;
    #     }
    # }
# EDITING END

# CRM START
    server {
        listen 80;
        server_name crm.edu-all.com;
        root /crm/public;
        index index.php index.html;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
            expires 60m;
            add_header Cache-Control "public, max-age=3600";
        }

        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }

        location ~ \.php$ {
            try_files $uri = 404;
            fastcgi_split_path_info ^(.+\.php)(/.+)$;
            fastcgi_pass php_crm:9000;
            fastcgi_index index.php;
            include fastcgi_params;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            fastcgi_param PATH_INFO $fastcgi_path_info;
            fastcgi_param HTTPS $http_x_forwarded_proto;
            fastcgi_param HTTP_X_FORWARDED_PROTO $http_x_forwarded_proto;
            fastcgi_connect_timeout 14400;
            fastcgi_send_timeout 14400;
            fastcgi_read_timeout 14400; 
        }
    }
# CRM END

# CRM STAGING START
    server {
        listen 80;
        server_name staging-crm.edu-all.com;
        root /staging-crm/public;
        index index.php index.html;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }

        location ~ \.php$ {
            try_files $uri = 404;
            fastcgi_split_path_info ^(.+\.php)(/.+)$;
            fastcgi_pass staging_php_crm:9000;
            fastcgi_connect_timeout 14400;
            fastcgi_send_timeout 14400;
            fastcgi_read_timeout 14400;  
            fastcgi_index index.php;
            include fastcgi_params;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            fastcgi_param PATH_INFO $fastcgi_path_info;
            fastcgi_param HTTPS $http_x_forwarded_proto;
            fastcgi_param HTTP_X_FORWARDED_PROTO $http_x_forwarded_proto;
        }
    }
# CRM STAGING END

# MENTORING BACK-END START
    server {
        listen 80;

        server_name api-mentoring.edu-all.com;
        root /mentoring/back-end/public;
        index index.php index.html;

        location / {
            add_header 'Access-Control-Allow-Origin' '*' always;
            try_files $uri $uri/ /index.php?$query_string;
        }

        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }

        location ~ \.php$ {
            try_files $uri = 404;
            fastcgi_split_path_info ^(.+\.php)(/.+)$;
            fastcgi_pass php_mentoring:9000;
            fastcgi_connect_timeout 14400;
            fastcgi_send_timeout 14400;
            fastcgi_read_timeout 14400;  
            fastcgi_index index.php;
            include fastcgi_params;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            fastcgi_param PATH_INFO $fastcgi_path_info;
            fastcgi_param HTTPS $http_x_forwarded_proto;
            fastcgi_param HTTP_X_FORWARDED_PROTO $http_x_forwarded_proto;
        }
    }
# MENTORING BACK-END END

# MENTORING FRONT-END START
    server {
        listen 80;

        server_name mentoring.edu-all.com;
        root /mentoring/front-end/dist;
        index index.php index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }
    }
# MENTORING FRONT-END END

# EDITING V2 BACK-END START
    server {
        listen 80;
        server_name api-editing.edu-all.com;
        root /editing_v2/back-end/public;
        index index.php index.html;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }

        location ~ \.php$ {
            try_files $uri = 404;
            fastcgi_split_path_info ^(.+\.php)(/.+)$;
            fastcgi_pass php_editing_v2:9000;
            fastcgi_connect_timeout 14400;
            fastcgi_send_timeout 14400;
            fastcgi_read_timeout 14400;  
            fastcgi_index index.php;
            include fastcgi_params;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            fastcgi_param PATH_INFO $fastcgi_path_info;
            fastcgi_param HTTPS $http_x_forwarded_proto;
            fastcgi_param HTTP_X_FORWARDED_PROTO $http_x_forwarded_proto;
        }
    }
# EDITING V2 BACK-END END

# EDITING V2 FRONT-END START
    server {
        listen 80;
        server_name editing.edu-all.com;
        root /editing_v2/front-end/dist;
        index index.php index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }
    }
# EDITING V2 FRONT-END END

# AUTHORIZATION START
    server {
        listen 80;
        server_name auth.edu-all.com;
        root /authorization/public;
        index index.php index.html;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
            expires 60m;
            add_header Cache-Control "public, max-age=3600";
        }

        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }

        location ~ \.php$ {
            try_files $uri = 404;
            fastcgi_split_path_info ^(.+\.php)(/.+)$;
            fastcgi_pass php_authorization:9000;
            fastcgi_index index.php;
            include fastcgi_params;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            fastcgi_param PATH_INFO $fastcgi_path_info;
            fastcgi_param HTTPS $http_x_forwarded_proto;
            fastcgi_param HTTP_X_FORWARDED_PROTO $http_x_forwarded_proto;
            fastcgi_connect_timeout 14400;
            fastcgi_send_timeout 14400;
            fastcgi_read_timeout 14400; 
        }
    }
# AUTHORIZATION END

# EDUALL ASSISTANT FRONT-END START
    server {
        listen 80;
        server_name assistant.edu-all.com;
        root /assistant/dist;
        index index.php index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }
        
        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }
    }
# EDUALL ASSISTANT FRONT-END END

# ELASTICSEARCH START
    # server {
    #     listen 443 ssl http2;
    #     server_name log.edu-all.com;

    #     ssl_certificate /etc/letsencrypt/live/edu-all.com-0002/fullchain1.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/edu-all.com-0002/privkey1.pem;

    #     location / {
    #         proxy_pass http://elasticsearch:9200;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #     }
    # }

    # server {
    #     listen 80;
    #     server_name log.edu-all.com;
    #     return 301 https://$server_name$request_uri;
    # }
# ELASTICSEARCH END 

# KIBANA START
    # server {
    #     listen 443 ssl http2;
    #     server_name monitoring.edu-all.com;

    #     ssl_certificate /etc/letsencrypt/live/edu-all.com-0002/fullchain1.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/edu-all.com-0002/privkey1.pem;

    #     location / {
    #         proxy_pass http://kibana:5601;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #     }
    # }

    # server {
    #     listen 80;
    #     server_name monitoring.edu-all.com;
    #     return 301 https://$server_name$request_uri;
    # }
# KIBANA END 

# N8N START
    server {
        listen 80;
        server_name workflow.edu-all.com;

        location / {
            proxy_pass http://n8n:5678;

            # --- WebSocket ---
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";

            # --- Header ---
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # --- Timeout (10 minutes = 600 seconds) ---
            proxy_connect_timeout       600s;
            proxy_send_timeout          600s;
            proxy_read_timeout          600s;
            send_timeout                600s;
        }

        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }
    }
# N8N END 

# ASSET MANAGEMENT START
    server {
        listen 80;
        server_name asset.edu-all.com;
        root /asset-management/public;
        index index.php index.html;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
            expires 60m;
            add_header Cache-Control "public, max-age=3600";
        }

        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }

        location ~ \.php$ {
            try_files $uri = 404;
            fastcgi_split_path_info ^(.+\.php)(/.+)$;
            fastcgi_pass php_asset:9000;
            fastcgi_index index.php;
            include fastcgi_params;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            fastcgi_param PATH_INFO $fastcgi_path_info;
            fastcgi_connect_timeout 14400;
            fastcgi_send_timeout 14400;
            fastcgi_read_timeout 14400; 
        }
    }
# ASSET MANAGEMENT END

# HELPDESK START
    server {
        listen 80;
        server_name helpdesk.edu-all.com;
        root /helpdesk;
        index index.php index.html;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
            expires 60m;
            add_header Cache-Control "public, max-age=3600";
        }

        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }

        location ~ \.php$ {
            try_files $uri =404;
            fastcgi_split_path_info ^(.+\.php)(/.+)$;
            fastcgi_pass php_helpdesk:9000;
            fastcgi_index index.php;
            include fastcgi_params;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            fastcgi_param PATH_INFO $fastcgi_path_info;
            fastcgi_connect_timeout 14400;
            fastcgi_send_timeout 14400;
            fastcgi_read_timeout 14400;
        }
    }
# HELPDESK END

# THE CORNERSTONE START
    server {
        listen 80;
        server_name www.thecornerstone.edu-all.com;

        return 301 https://thecornerstone.edu-all.com$request_uri;
    }
# THE CORNERSTONE END
